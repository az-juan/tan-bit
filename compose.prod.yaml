services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.prod
    restart: unless-stopped
  database:
    image: postgres:15-alpine
    restart: unless-stopped
  broker:
    image: nats:2.15-alpine
    restart: unless-stopped
